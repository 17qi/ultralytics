# 模块: 过期缓存检查

目标: 验证是否因缓存导致训练读取不到标签。

## 最小任务清单
- [x] 查找数据根目录与 runs 目录下的 .cache 文件。
- [x] 记录缓存文件路径与时间戳。
- [x] 评估是否与当前数据路径或分割不一致。
- [x] 记录处理建议(仅记录, 不删除)。

## 验证记录
- 删除 images/train.cache 与 images/val.cache (重命名目录后缓存已过期)
